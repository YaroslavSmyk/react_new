import React from 'react';

const GoodButton = () => {
  return (
    <button className="fance-button" onClick={() => alert('Good job!')}>
      Click me!
    </button>
  );
};

export default GoodButton;
